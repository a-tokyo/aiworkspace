# Copy this file to .env.local (gitignored) at the parent workspace root and fill in your tokens.
# Restart Cursor / Claude Code after editing — MCP loads secrets from .env.local automatically.

# GitHub MCP (stdio) — create at https://github.com/settings/tokens
GITHUB_PERSONAL_ACCESS_TOKEN=
# Legacy alias (also accepted): GITHUB_PAT=
