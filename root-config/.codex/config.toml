# Team Codex defaults (preamble) — safe project keys only. Edit here.
# [mcp_servers.*] blocks below are regenerated from .agents/mcp.json on `npm run sync`.
# Personal model, provider, and auth: ~/.codex/config.toml (never mirrored).

project_doc_fallback_filenames = ["AGENTS.md", "CLAUDE.md", "CONTINUING.md"]

experimental_use_rmcp_client = true

[mcp_servers.context7]
url = "https://mcp.context7.com/mcp/oauth"
